openapi: 3.1.0
info:
  version: 1.0.0
  title: Contacts API
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description: >
    Contacts API documentation generated for hw7-swagger task.

tags:
  - name: Contacts
    description: Operations related to contacts
  - name: Auth
    description: Authentication endpoints

servers:
  - url: http://localhost:3000
  - url: https://your-render-domain.onrender.com

paths:
  /contacts:
    get:
      $ref: ../swagger/paths/contacts/getAll.yaml
    post:
      $ref: ../swagger/paths/contacts/create.yaml

  /contacts/{contactId}:
    get:
      $ref: ../swagger/paths/contacts/id/get.yaml
    patch:
      $ref: ../swagger/paths/contacts/id/update.yaml
    delete:
      $ref: ../swagger/paths/contacts/id/delete.yaml
