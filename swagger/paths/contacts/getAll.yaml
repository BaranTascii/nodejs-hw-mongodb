get:
  tags:
    - Contacts
  summary: Get all contacts
  operationId: getContacts
  security:
    - bearerAuth: []
  parameters:
    - in: query
      name: page
      schema:
        type: integer
    - in: query
      name: limit
      schema:
        type: integer
  responses:
    '200':
      description: List of contacts
